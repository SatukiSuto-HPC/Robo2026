# =============================================================================
# a_mu.py - a-mu.py のエイリアス/ラッパーモジュール
# =============================================================================
# Pythonのインポート構文でハイフンを含むモジュール名 (a-mu) を直接指定できないため、
# 通常の `import a_mu` や `from a_mu import ArmController` を行えるように提供しています。
# =============================================================================

import sys

# ハイフン付きファイル 'a-mu.py' を動的に読み込み
_module = __import__('a-mu')

ArmController = _module.ArmController
Arm = _module.Arm

if __name__ == '__main__':
    arm = ArmController()
    arm.begin()
